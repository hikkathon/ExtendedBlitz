<Window x:Class="ExtendedBlitz.Views.Windows.StatSessionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExtendedBlitz.Views.Windows" 
        xmlns:vw="clr-namespace:ExtendedBlitz.ViewModels"
        mc:Ignorable="d"
        Title="StatSessionView" Height="200" Width="300"
        Topmost="True"
        WindowStartupLocation="Manual"
        AllowsTransparency="True"
        WindowStyle="None" ShowInTaskbar="True">

    <Window.Background>
        <SolidColorBrush Opacity="0.0" Color="White"/>
    </Window.Background>

    <Window.DataContext>
        <vw:StatSessionViewModel/>
    </Window.DataContext>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="15"/>
    </WindowChrome.WindowChrome>

    <Grid Visibility="{Binding VisibilityStats}">
        <Label Foreground="White" FontSize="14" Content="{Binding Stats}">
            <Label.Effect>
                <DropShadowEffect
                              ShadowDepth="4"
                              Direction="330"
                              Color="Black"
                              Opacity="0.5"
                             BlurRadius="4"/>
            </Label.Effect>
        </Label>
    </Grid>
</Window>
